# ============================================================================
# MURMURATION - ENVIRONMENT VARIABLES
# ============================================================================
# Copy this file to .env and replace with your actual API keys and configuration
# Make sure to add .env to your .gitignore file to keep your keys secure
#
# Required: At least one API key must be provided for the providers you wish to use
# ============================================================================

# ============================================================================
# PROVIDER API KEYS
# ============================================================================

# ----------------------------------------------------------------------------
# Anthropic API Configuration (https://console.anthropic.com/settings/keys)
# ----------------------------------------------------------------------------
# Required for Claude models
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Override the default API URL (for enterprise or proxy setups)
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# Optional: Request timeout in seconds (default: 60)
# ANTHROPIC_TIMEOUT=60

# ----------------------------------------------------------------------------
# OpenAI API Configuration (https://platform.openai.com/api-keys)
# ----------------------------------------------------------------------------
# Required for GPT models
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Organization ID for enterprise plans
# OPENAI_ORGANIZATION=org-your-org-id

# Optional: Custom API URL (for Azure OpenAI or proxy)
# OPENAI_BASE_URL=https://api.openai.com/v1

# ----------------------------------------------------------------------------
# Google Gemini API Configuration (https://aistudio.google.com/app/apikey)
# ----------------------------------------------------------------------------
# Required for Gemini models
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Custom API URL
# GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# Logging level: debug, info, warning, error (default: info)
LOG_LEVEL=info

# Enable debug mode (true/false)
DEBUG=false

# Cache configuration (in seconds, 0 to disable)
CACHE_TTL=3600

# Maximum retry attempts for API calls (default: 3)
MAX_RETRIES=3

# Request timeout in seconds (default: 30)
REQUEST_TIMEOUT=30

# ============================================================================
# ADVANCED CONFIGURATION
# ============================================================================
# The following settings are for advanced use cases and can usually be left as default

# Enable/disable telemetry (true/false)
# TELEMETRY_ENABLED=true

# Custom user agent for API requests
# USER_AGENT=Murmuration/3.1.0

# Proxy configuration (if needed)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1

# ============================================================================
# SECURITY NOTES
# ============================================================================
# 1. Never commit your .env file to version control
# 2. Use environment variables or a secrets manager in production
# 3. Rotate your API keys regularly
# 4. Follow the principle of least privilege when generating API keys
# ============================================================================
